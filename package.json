{
  "name": "@sayjava/behave-project",
  "private": true,
  "license": "ISC",
  "keywords": [
    "mock-server",
    "mock-services",
    "mock-api",
    "http stub",
    "http-mock-server",
    "express-mock-middleware",
    "http-test-server"
  ],
  "description": "A robust HTTP mocking server to rapidly aid application development and system testing",
  "repository": "git@gitlab.com:sayjava/behave.git",
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "bootstrap": "lerna bootstrap",
    "prepublish": "yarn build",
    "release": "yarn build && lerna publish",
    "lint": "eslint '*/**/src/*.ts' --quiet --fix",
    "dev:docs": "vuepress dev docs",
    "build:docs": "vuepress build docs"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "@typescript-eslint/eslint-plugin": "^4.11.1",
    "@typescript-eslint/parser": "^4.11.1",
    "eslint": "^7.17.0",
    "eslint-config-prettier": "^7.1.0",
    "eslint-plugin-prettier": "^3.3.0",
    "lerna": "^3.22.1",
    "prettier": "^2.2.1",
    "vuepress": "^1.5.3"
  },
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "rome": {
    "version": "^0.0.1"
  },
  "dependencies": {}
}
