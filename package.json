{
  "name": "behave",
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "lerna": "^3.22.1"
  }
}
