{
  "name": "@sayjava/behave",
  "private": "true",
  "license": "ISC",
  "keywords": [
    "mock-server",
    "mock-services",
    "mock-api",
    "http stub",
    "http-mock-server",
    "express-mock-middleware",
    "http-test-server"
  ],
  "description": "An HTTP mocking and testing server. Quickly stub out network response to requests",
  "repository": "git@gitlab.com:sayjava/behave.git",
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "bootstrap": "lerna bootstrap",
    "prepublish": "yarn build",
    "release": "yarn build && lerna publish"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "lerna": "^3.22.1"
  },
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ]
}
