name: "Investigating nonesense claims"

description: "A sample test that works well"

expectations:
  - request:
      path: /todo/new
      method: POST
      body:
        entity:
          id: new_id
          text: "A new todo post"

    response:
      statusCode: 200
      body:
        entity:
          desc: "Worked well"
      headers:
        X-Flyt-Special: "Super Header"
        X-Flyt-Session: "Flyt Session"

    times:
      remainingTimes: 2
      unlimited: false

  - request:
      path: /post/^(id)/
      method: DELETE
    response:
      statusCode: 200
      body:
        entity:
          reason: "Deleted post"

      times:
        remainingTimes: 2
        unlimited: false

      delay: 10
        
